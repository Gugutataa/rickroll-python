requires:

only works if opera gx is opened, also you need to disable windows minimize animations by typing advanced system settings into search button, under performance tab disable minimize and maximize animations

pip install pyautogui
pip install pywin32

